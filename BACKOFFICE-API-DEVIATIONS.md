# 백오피스 API 명세와 서버 규약의 차이

프론트엔드가 전달한 백오피스 API 명세 중, 이 레포의 기존 규약과 어긋나거나 명세를 그대로 따를 수 없었던 항목을 모았다.
`.claude/rules/code-convention/`, `.claude/spec/`이 서버 규약의 출처다.

각 항목은 셋 중 하나다.

- **명세를 따랐다** - 규약에서 벗어나지만 클라이언트 계약이 우선이라 명세대로 구현했다
- **규약을 따랐다** - 명세와 다르게 구현했다. **프론트엔드가 알아야 한다**
- **명세에 없다** - 명세가 다루지 않아 서버가 정한 것. **프론트엔드가 알아야 한다**

---

## 1. 명세를 따랐다 (규약에서 벗어남)

### 1-1. `/api/v1/admin/...` 네임스페이스 접두사

컨벤션은 `@RequestMapping("/api/v1/{도메인복수형}")`이라 경로 첫 마디가 도메인이어야 한다. `admin`은 도메인이 아니라 권한 경계다.
그럼에도 백오피스 전체를 한 접두사 아래 두면 인증 필터를 경로 하나로 가를 수 있어 명세를 따랐다.

### 1-2. 리소스 명사가 아닌 하위 경로

`courses/summary`, `sync/preflight`는 리소스가 아니라 화면 관심사다.
`sync`는 복수형 명사도 아니다. 명세대로 두되, 이후 백오피스 API를 늘릴 때 이 패턴을 기준으로 삼지 않는다.

표시 학기의 `semesters/display`만 규약에 맞춰 바로잡았다. 2-0을 본다.

### 1-3. 페이지네이션 래퍼의 `content`

DTO 컨벤션은 목록 컴포넌트명을 `{단수형}Responses`로 통일한다(`majorCourseResponses`).
명세가 `content`로 고정해 공통 래퍼 `PageResponse`는 `content`를 쓴다. 학생 API의 목록 응답은 기존 규칙을 유지한다.
**한 서버 안에 두 규칙이 공존한다.**

### 1-4. 에러 메시지 어투

기존 `ExceptionCode`는 전부 `~습니다`체다(`사용자를 찾을 수 없습니다.`).
명세의 5000번대는 `~에요`체라 그대로 넣었다. **같은 클라이언트가 두 어투를 함께 받는다.**
학생 API 문구는 바꾸지 않았다. 백오피스와 학생 화면이 분리돼 있어 사용자가 섞인 문구를 볼 일은 없다.

### 1-5. 202 상태코드

기존 API에 없던 상태코드다. 비동기 작업 생성에만 쓴다.

---

## 2. 규약을 따랐다 (프론트엔드 확인 필요)

### 2-0. 표시 학기 엔드포인트에서 `/display`를 뺐다 (호출 경로 변경)

> **클라이언트 수정이 필요한 유일한 항목이다.** 나머지 9개 엔드포인트는 명세 그대로다.

| 용도 | 명세 | **실제** |
|---|---|---|
| 표시 학기 조회 | `GET /api/v1/admin/semesters/display` | **`GET /api/v1/admin/semesters`** |
| 표시 학기 변경 | `PUT /api/v1/admin/semesters/display` | **`PUT /api/v1/admin/semesters`** |

`display`는 리소스가 아니라 용도를 가리키는 말이라, 컨벤션(경로는 리소스 명사)에서 벗어난다.
표시 학기 리소스는 서버 전체에 하나뿐이고 조회와 변경만 있으므로 `semesters`가 그 리소스를 그대로 가리킨다.

**요청 본문, 응답 본문, 상태코드, 에러코드는 명세와 완전히 같다.** 바뀐 것은 경로뿐이다.

```
GET  /api/v1/admin/semesters        -> 200 { "academicYear": 2026, "term": "SECOND" }
PUT  /api/v1/admin/semesters        -> 200 { "academicYear": 2026, "term": "SECOND" }
     body: { "academicYear": 2026, "term": "SECOND" }
```

### 2-1. 신규 강의 수강 정원 기본값은 40이 아니라 **100**이다

> 명세: "신규 생성 시 `maxCapacity` 기본 40"

기존 시드로 적재된 강의 전체가 100이고 `.claude/spec/service-policy/course.md`도 100으로 정하고 있다.
40을 적용하면 **동기화로 새로 생긴 강의만 정원이 달라져 한 화면에 두 기준이 섞인다.**
정원은 학교 API가 주지 않는 서비스 소유 값이라 운영자가 조정하는 것이 전제다.

**명세의 40이 근거 있는 값이면 알려달라.** 그때는 기존 데이터까지 함께 바꾼다.

### 2-2. `changedFields[].field` 식별자 목록이 명세와 다르다

명세 §6-2는 `Course` 엔티티 필드명을 쓴다고 했지만, 나열된 이름 일부가 현재 엔티티와 맞지 않는다.
이슈 #79에서 이수구분, 수업유형, 학년이 enum에서 원문 코드와 명칭 쌍으로 바뀌었기 때문이다.

**실제로 내려가는 식별자는 아래가 전부다.**

| 식별자 | 값 | 명세 대비 |
|---|---|---|
| `titleKr` | 과목명(국문) | 동일 |
| `titleEn` | 과목명(영문) | 동일 |
| `courseCode` | 과목코드 | 동일 |
| `credits` | 학점 | 동일 |
| `college` | 단과대학 한글명 | 동일 |
| `department` | 학과 한글명 | 동일 |
| `classification` | 이수구분 **명칭** | enum이 아니라 원문 명칭 문자열 |
| `area` | 이수영역 **명칭** | enum이 아니라 원문 명칭 문자열 |
| `type` | 수업유형 **명칭** | enum이 아니라 원문 명칭 문자열 |
| `grade` | 학년 **명칭** | enum이 아니라 원문 명칭 문자열(`2학년`) |
| `isEnglishCourse` | 원어강의 **구분명** | `true`/`false`가 아니라 `비대상`, `원어강의(EN)` 같은 명칭 |
| `isHussCourse` | `true`/`false` | 동일 |
| `schedule` | 시간표 전체 포맷 문자열 | 동일 |
| `concentration` | 집중이수제 명칭 | **명세에 없음. 추가됨** |
| `status` | `CLOSED` -> `ACTIVE` | **명세에 없음. 추가됨** (아래 2-3) |

`maxCapacity`와 `currentEnrollment`는 명세대로 절대 나타나지 않는다.

### 2-3. 폐강 후 재개설을 `status` 변경으로 남긴다

폐강된 강의가 다음 동기화에서 다시 수집되면 되살리고, 수정 항목으로 잡아 `changedFields`에 `status`를 넣는다.
명세에 없는 상황이라 서버가 정했다. 조용히 되살리면 관리자가 복구 사실을 알 방법이 없다.

### 2-4. 폐강 강의는 학생 조회에서 제외된다

> 명세 §8: "신청 로직에 `status = ACTIVE` 검증으로 방어한다"

신청 검증만으로는 부족하다고 판단해 **학생 조회 4종(전공, 교양, 타학과와 연계전공, 검색, HUSS)에서도 제외**했다.
목록에 남겨두면 학생이 신청을 시도했다 실패하는데, 강의 응답에 폐강 여부 필드가 없어 프론트가 이유를 표시할 수도 없다.

담기와 신청은 신규 에러코드로 막는다(2-5).

### 2-5. 신규 에러코드 `2003`

| code | HTTP | message | 발생 |
|---|---|---|---|
| `2003` | 400 | 폐강된 과목입니다. | 폐강 강의를 장바구니에 담거나 수강신청할 때 |

명세에 없는 코드다. 학생 API 대역(2000번대)이라 백오피스 5000번대에 넣지 않았다.
취소와 삭제는 막지 않으므로 이 코드가 나오지 않는다.

### 2-6. 요청 본문의 잘못된 enum이 이제 `8888`로 내려간다

명세 요구대로 맞췄지만, 기존에는 `9999`(서버 내부 오류)로 떨어지던 경로다.
**학생 API에도 함께 적용된다.** 이전에 500을 받던 잘못된 요청이 이제 400을 받는다.

### 2-7. 테이블명은 복수형이다

명세는 `semester_setting`, `course_sync_job`, `course_sync_detail`로 적었지만 레포의 기존 테이블이 전부 복수형이라
`system_semesters`, `course_sync_jobs`, `course_sync_details`로 만들었다. API 계약에 드러나지 않는다.

표시 학기는 이름도 바꿨다. `semester_setting`의 "설정"은 무엇의 설정인지 드러내지 않아 `system_semesters`로 두고,
엔티티도 `SystemSemester`로 부른다. 응답 필드(`academicYear`, `term`)는 그대로다.

### 2-8. `course_sync_detail.applied` 컬럼을 만들지 않았다

명세 §8이 2단계 적용 흐름이 없다고 못박았으므로 이 값은 항상 `true`다. 값이 하나뿐인 컬럼은 이력이 아니다.

---

## 3. 명세에 없어 서버가 정한 것

### 3-1. 관리자 토큰과 학생 토큰의 교차 사용을 막는다

두 토큰은 **같은 서명 키**를 쓰고 `subject`에 각각 `admins.id`와 `members.id`를 담는다.
막지 않으면 관리자 토큰으로 학생 API를 호출했을 때 서버가 그 값을 회원 식별자로 읽어 **남의 데이터를 내준다.**

- 학생 API에 관리자 토큰을 보내면 `401` / `1001`
- 백오피스 API에 학생 토큰을 보내면 `403` / `5002`

### 3-2. `page` 파라미터 하한

`page`가 1 미만이면 `400` / `7777`이다. 명세가 다루지 않았다.

### 3-3. `partiallyApplied`는 사실상 항상 `false`다

적재를 트랜잭션 하나로 처리해 도중에 실패하면 전부 되돌아간다.
`true`가 되는 경우는 **적재가 커밋된 뒤 작업 상태를 기록하다 실패**했을 때뿐이다.

### 3-4. 경고(WARNING)가 생기는 조건

명세는 `"미등록 이수구분 코드: 99"`를 예로 들었지만 실제 발생 지점은 다섯이다.

| 사유 문구 | 원인 |
|---|---|
| `미등록 단과대학 코드: {값}` | `COLLEGE_CODE`가 enum에 없음 |
| `미등록 학과 코드: {값}` | `DEPT_CODE`가 enum에 없음 |
| `미등록 이수영역 코드: {값}` | `ISU_FLD_CODE`가 enum에 없음 |
| `미등록 요일 코드: {값}` | `DAY_CODE`가 enum에 없음 |
| `해석할 수 없는 교시 시각: {값}` | `LECTM_START`, `LECTM_END`가 시각 형식이 아님 |

앞의 셋은 **강의 전체를 건너뛴다.** 뒤의 둘은 **그 시간표 한 줄만 버리고 강의는 적재한다.**

### 3-5. 이수구분과 수업유형은 경고 대상이 아니다

명세의 예시(`"미등록 이수구분 코드: 99"`)와 다르다. 이수구분, 수업유형, 학년, 집중이수제는 enum으로 검증하지 않고
학교가 준 코드와 명칭을 원문 그대로 저장한다(#79에서 정한 방향). 모르는 코드가 와도 그대로 적재된다.

---

## 4. 서버에서 검증되지 않은 것

### 4-1. 학교 연계 API 실호출

연계 API는 **등록된 IP에서만 호출된다.** 개발 환경에서 시도하면 `401 Unauthorized IP`가 돌아오고,
IP 검사가 키 검사보다 앞서므로 인증 키 유효성도 확인할 수 없다.

따라서 아래는 **운영 배포 후에야 확인 가능하다.**

- 인증 키가 유효한지
- POST + `AUTH_KEY` 헤더 + 쿼리 파라미터 조합이 맞는지
- `totalpageSize`가 실제로 내려오는지 (2026-08-04 실측에서는 `null`이었고 전체가 1페이지에 왔다)
- 전체 수집에 걸리는 시간

서버는 호출부를 인터페이스로 분리해두었고 테스트는 가짜 구현으로 돈다.
**첫 동기화는 운영에서 실행해보고 결과를 확인해야 한다.**
