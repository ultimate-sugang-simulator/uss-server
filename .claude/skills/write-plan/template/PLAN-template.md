# [PLAN-{번호}] {제목}

> 이슈: #{번호}
> 브랜치: {브랜치명}

## 목표
{이슈에서 파악한 "무엇을/왜". 1~2문장}

## 영향 범위
### 신규 파일
- `{경로}` — {역할}

### 수정 파일
- `{경로}` — {무엇을 어떻게 바꾸는지}

## 구현 계획
> 레이어 순으로, 클래스·메서드 단위까지 구체적으로. "적절히 구현한다" 같은 추상 표현 금지.
1. **Entity / Flyway**: {DB 변경 시 마이그레이션 파일명·컬럼}
2. **Repository**: `{Repo}.{메서드시그니처}` — {쿼리 요지}
3. **Service**: `{Service}.{메서드시그니처}` — {로직 요지}
4. **DTO**: `{Request/Response}` — {필드}
5. **Controller**: `{HTTP} {경로} → {메서드}`

## 결정 필요 (Decisions needed)
> 계획 단계에서 사용자와 확정해야 할 미결정 사항. write-plan 스킬이 여기서 확정하고 체크한다.
- [ ] {미결정 사항} — 옵션 A / 옵션 B

## 검증
- 대상 테스트: {테스트 클래스·시나리오}

## Deviation Log
> implement 스킬이 구현 중 계획을 벗어난 지점을 여기에 기록한다. (작성 시점엔 비워둔다)
