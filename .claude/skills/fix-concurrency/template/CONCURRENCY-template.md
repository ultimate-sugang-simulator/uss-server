# [CONC-{이슈번호}] {대상 임계 구역}

> 이슈: #{이슈번호}
> 브랜치: {브랜치명}
> 대상 디렉토리: `.claude/resources/concurrency/{이슈번호}/{슬러그}/`

이 파일은 임계 구역 하나만 다룬다. 같은 이슈의 다른 임계 구역은 각자의 디렉토리에 각자의 `record.md`를 가진다.

## 진행 상태

> ⏳ 미완 / ✅ 완료 / ⏭️ 건너뜀
> 재진입 시 ⏳로 표기된 가장 이른 Phase부터 재개한다.

**준비 (대상당 1회)**

| 1. 불변식 | 2. 환경 | 3. 경합 조건 | 4. 재현 |
|---|---|---|---|
| ⏳ | ⏳ | ⏳ | ⏳ |

**후보 (반복)**

| # | 기법 | 5. 설계 | 6. 적용 | 7. 검증 |
|---|---|---|---|---|
| 1 | | ⏳ | ⏳ | ⏳ |

**마무리**

| 8. 채택 |
|---|
| ⏳ |

## 대상

- 임계 구역: `{클래스.메서드}` ({파일}:{시작줄}-{끝줄})
- 공유 자원: {경합하는 행, 컬럼, 집계 값}
- read-modify-write 구간: {읽는 지점} → {판단하는 지점} → {쓰는 지점}
- 실제 발행 SQL: `{더티 체킹이 만드는 UPDATE 원문}`
  - 절대값 대입인가 증감식인가: {절대값 / 증감식}
- 트랜잭션 경계: {어디서 열리고 닫히는지}
- 기존 방어 장치

  | 수단 | 유무 | 막아주는 범위 |
  |---|---|---|
  | `@Version` | | |
  | 비관적 락 | | |
  | UNIQUE 제약 | | |
  | CHECK 제약 | | |

## 불변식

> Phase 1에서 호출자와 확정한 것. **후보마다 바꾸지 마라.**
> 검증 SQL은 `{슬러그}/invariant-check.sql`에 저장하고 Phase 4, 7에서 그대로 재사용한다.

| # | 불변식 | 검증 방법 | 깨졌을 때의 증상 |
|---|---|---|---|
| I1 | {예: 등록 행 수 <= 정원} | {SQL 요지} | {예: 정원 초과 등록} |
| I2 | {예: 집계 컬럼 = COUNT(실제 행)} | {SQL 요지} | {예: 갱신 유실} |
| I3 | {예: 집계 컬럼 >= 0} | {SQL 요지} | {예: 음수 인원} |

응답 코드 기대치: {정원 초과 시 기대하는 상태 코드와 에러 코드}

## 측정 환경

> 값이 바뀌면 그 사실을 후보 기록에 남긴다.

| 항목 | 값 |
|---|---|
| 프로파일 | conc (`application-conc.yml`) |
| DB | MySQL 8.0 / InnoDB (`127.0.0.1:3307`, `uss_db`) |
| 커넥션 풀 크기 | {maximum-pool-size} |
| 격리 수준 | {@@transaction_isolation} |
| 락 대기 타임아웃 | {@@innodb_lock_wait_timeout}초 |
| 관측 가능한 락 지표 | {Innodb_row_lock% / INNODB_METRICS / data_locks 중 실제로 읽힌 것} |
| 캐시 상태 | warm 고정. InnoDB 버퍼 풀은 재기동 없이 비울 수 없고 애플리케이션 캐시는 쓰지 않는다 |

## 경합 조건

> Phase 3에서 확정. **모든 후보가 이 조건을 그대로 쓴다.**

| 항목 | 값 |
|---|---|
| 다투는 자원 | {대상 강의 id 등, 1개} |
| 허용 상한 | {정원 등} |
| 동시 요청 수 (VU) | {n} |
| 회원 수 | {VU와 같아야 한다} |
| executor | `per-vu-iterations`, `iterations: 1` (ramp 없음) |
| 시드 SQL | `../seeds.sql` (이슈 공용) |
| 시드 모듈과 변수 | {`SOURCE`로 부른 모듈과 확정한 변수값} |
| 측정용 토큰 | `../tokens.json` (서명키로 직접 생성, 로그인 API 미사용) |

**되돌리기 SQL** (매 측정 전에 실행)

```sql
DELETE FROM registrations WHERE course_id = {대상 강의 id};
UPDATE courses SET current_enrollment = 0 WHERE id = {대상 강의 id};
```

## 재현 (Baseline, `-0`)

> 원본: `invariant-0.txt` / `k6-burst-summary-0.json` / `lock-stats-0.txt`
> 여기에는 판정 근거로 쓴 값만 옮긴다. 전체를 복사하지 않는다.

**정합성**

| 불변식 | 기대 | 실측 | 위반 |
|---|---|---|---|
| I1 | | | |
| I2 | | | |
| I3 | | | |

**응답 분포**

| 상태 코드 | 건수 | 의미 |
|---|---|---|

**경합**

| 지표 | BEFORE | AFTER | 증가분 |
|---|---|---|---|
| `Innodb_row_lock_waits` | | | |
| `Innodb_row_lock_time_avg` | | | |
| `lock_deadlocks` | | | |
| `lock_timeouts` | | | |

**진단**

- 재현 여부: {재현됨 / 실패}
- 결함 성격: {Phase 4에서 확정한 판정}
- 근거: {관측된 수치. 특히 카운터 정합 오차}
- 추정 인터리빙: {요청들이 어떤 순서로 겹쳐야 이 수치가 나오는지}

---

## 후보 {n}: {기법}

### 설계 결정

> Phase 5-B에서 호출자와 확정한 내용.

| 결정 항목 | 정한 값 | 근거 |
|---|---|---|

- 검토했지만 택하지 않은 안: {안} - {배제 근거}
- 호출자가 예상한 결과
  - 정합성: {어느 불변식이 어떻게 지켜지는가}
  - 경합: {처리량이 어느 방향으로 얼마나 움직일 것 같은가}

### 적용 내용

- {수정한 파일과 변경 요지}
- 제어 발행 확인: {`performance_schema` digest에서 확인한 문장 형태}
- 테스트: {`./gradlew test` 결과}
- 추가한 마이그레이션: {파일명 / 없으면 `없음`}

### 검증 결과

**정합성 (1급)**

| 불변식 | 원본(`-0`) 위반 | 후보(`-{n}`) 위반 | 판정 |
|---|---|---|---|
| I1 | | | |
| I2 | | | |
| I3 | | | |

판정: {정합성 확보 / 실패 - 근거}

**경합 (2급)**

| 지표 | 원본(`-0`) | 후보(`-{n}`) | 변화 |
|---|---|---|---|
| 성공 요청 수 | | | |
| RPS | | | |
| p95 / p99 | | | |
| `Innodb_row_lock_waits` 증가분 | | | |
| `Innodb_row_lock_time_avg` | | | |
| `lock_deadlocks` 증가분 | | | |
| `lock_timeouts` 증가분 | | | |
| 재시도 횟수 | | | |

- 응답 코드 품질: {거절이 의도한 코드로 나가는가, 500이 섞이는가}
- 예상과의 대조: {Phase 5-B의 예상이 맞았는지, 어긋났으면 어느 가정이 틀렸는지}

### 되돌리기

- stash: `{git stash list의 해당 항목}`
- 스키마 되돌리기: {실행한 SQL / 없으면 `해당 없음`}
- 데이터 복원 확인: `rows_left = 0`, `counter = 0` {확인 / 미확인}

---

## 최종 요약

### 후보 비교

| # | 기법 | 불변식 위반 | 성공 요청 수 | RPS | p99 | 락 대기 | 데드락 | 응답 코드 |
|---|---|---|---|---|---|---|---|---|
| 0 | (원본) | | | | | | | |

> 정합성을 확보하지 못한 후보도 표에 남긴다. 무엇이 안 통했는지가 근거다.

### 채택

- 채택한 기법: {기법}
- 채택 근거: {호출자가 든 근거}
- 배제한 후보와 이유: {후보별로}
- 최종 검증: `invariant-final.txt` - 위반 {n}건

### 운영 반영 시 유의점

| 확인 | 내용 |
|---|---|
| 마이그레이션 | {추가한 파일과 영향 / `스키마 변경 없음`} |
| 인프라 | {추가로 필요한 것 / `추가 없음`} |
| 기존 데이터 | {어긋난 카운터 보정이 필요한지} |
| 인스턴스 수 | {다중 인스턴스에서도 성립하는지} |
